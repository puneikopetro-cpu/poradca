[build]
builder = "NIXPACKS"

[deploy]
startCommand = "alembic upgrade head 2>&1 && echo 'Alembic done, starting uvicorn...' && uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000} 2>&1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
